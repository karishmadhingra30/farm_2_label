# Cached Wikidata responses. These are reproducible by re-running the
# pipeline, and they would otherwise add noise to every diff.
data/cache/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
